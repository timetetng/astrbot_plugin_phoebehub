{
  "cache_ttl": {
    "description": "数据缓存时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "memes.json 的缓存时间，减少重复请求。设为 0 关闭缓存。"
  },
  "download_local": {
    "description": "是否下载图片到本地缓存",
    "type": "bool",
    "default": false,
    "hint": "开启后，首次请求的图片会下载到 data/plugin_data/astrbot_plugin_phoebehub/images/，后续同一张图走本地文件，不再走 HTTP。"
  },
  "trigger_keywords": {
    "description": "触发关键词列表",
    "type": "list",
    "default": ["啾比", "jiubi", "jbi"],
    "hint": "消息内容精确匹配这些关键词之一时触发随机表情包。"
  },
  "proxy": {
    "description": "HTTP 代理地址（可选）",
    "type": "string",
    "default": "",
    "hint": "如 http://127.0.0.1:7890。留空不使用代理。"
  }
}
