{
  "cache_ttl": {
    "description": "memes.json 缓存时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "设为 0 关闭缓存。"
  },
  "trigger_keywords": {
    "description": "触发关键词列表",
    "type": "list",
    "default": [
      "啾比"
    ],
    "hint": "消息内容精确匹配这些关键词之一时触发随机表情包。"
  },
  "proxy": {
    "description": "HTTP 代理地址",
    "type": "string",
    "default": "",
    "hint": "如 http://127.0.0.1:7890。用于 memes.json 和图片下载。留空直连。"
  },
  "cache_max_hours": {
    "description": "本地图片缓存保留时长（小时）",
    "type": "int",
    "default": 24,
    "hint": "超过此时间的本地缓存文件会被自动清理。设为 0 不自动清理。"
  }
}
